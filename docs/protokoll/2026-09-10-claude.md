## Datum: 2026-09-10
- Wer: Claude Code, Sitzung mit David
- Was geändert: Repository frisch geklont auf Stand `06ead96`. `firebase-tools` global installiert und angemeldet. MCP-Server für GitHub und Firebase eingerichtet. Eigene `AGENTS.md`, `CLAUDE.md` und ein Protokollordner `docs/log/` angelegt – diese sind beim Zusammenführen am 2026-09-16 durch Pauls Fassung vom 2026-09-14 ersetzt worden, der Inhalt steht jetzt hier. `.superpowers/` in `.gitignore` eingetragen.
- Was verifiziert (mit Ausgabe): `node --test tests/*.test.cjs`: 36 von 36 bestanden, 261 ms. `firebase login:list`: angemeldet als `daveghg03@gmail.com`. `firebase projects:list`: Projekt `fahrklar-4c32c`, Nummer `553567497097`. Beide MCP-Server meldeten `Connected`. Android-Werkzeugkette nicht ausgeführt, weil lokal kein SDK und kein `gradle` vorhanden war.
- Was offen: Schlüssel des Dienstkontos `claude-fahrklar` ist kompromittiert und muss in der Google Cloud Console gelöscht werden. Fragenkatalog rechtlich offen. Release-Keystore war zu diesem Zeitpunkt offen; laut Codex-Protokoll vom 2026-09-14 inzwischen erledigt.
- Entscheidung: Designrichtung „zwei Register in einem Designsystem" – verspielt für Start, Pfad und Fortschritt, ruhig für Lernrunde und Prüfung. Vor jedem Umbau der Oberfläche ein Screenshot-Netz als Referenz.
- Warum: Lern- und Startbildschirm haben unterschiedliche Aufgaben, trugen aber dieselbe Verpackung. Ohne Referenzbilder fällt nicht auf, wenn eine CSS-Änderung andere Bildschirme mitverschiebt.
