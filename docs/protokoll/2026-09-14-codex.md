## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Gemeinsame Arbeitsregeln in `AGENTS.md`, Claude-Verweis in `CLAUDE.md` und konfliktarme getrennte Protokollführung unter `docs/protokoll/` angelegt.
- Was verifiziert (mit Ausgabe): Repository-Dateien und bestehende CI-Konfiguration geprüft. In `.github/workflows/android.yml` stehen die Android-Tests und der APK-Build; in `.github/workflows/web.yml` stehen `npm test` und `npm run build:web`. Die letzten bekannten Android- und Web-Läufe waren erfolgreich.
- Was offen: Schriftliche Nutzungsgenehmigung für den Fragenkatalog; dauerhafter Release-Keystore; veröffentlichte Web-Domain für zusätzliche Firebase-Domainfreigabe.
- Entscheidung: Pro Werkzeug eine eigene Tagesdatei statt einer gemeinsamen Protokolldatei.
- Warum: Parallele Anhänge erzeugen sonst unnötige Git-Konflikte; getrennte append-only Dateien bleiben konfliktarm und trotzdem nachvollziehbar.

## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Themenauswahl als fokussiertes Akkordeon verbessert; beim Öffnen einer Gruppe schließen sich andere Gruppen automatisch.
- Was verifiziert (mit Ausgabe): Patch auf `app/src/main/assets/app.js` erfolgreich veröffentlicht; GitHub meldete Commit `36fae5c75ef87983f80399b0e2cc107c0fed6bcc`.
- Was offen: Android-/Web-CI-Läufe dieses neuen Commits; Release-Keystore und Fragenfreigabe bleiben offen.
- Entscheidung: Kleine, barrierearme UX-Verbesserung ohne neue Abhängigkeit.
- Warum: Lange Themenlisten werden schneller scanbar und die Auswahl bleibt auf Mobilgeräten übersichtlich.

## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Ersten Start als dreistufige Einrichtung mit großen Klassenkarten, klarer Download-Aktion und ausgeblendeter Hauptnavigation ohne Katalog gestaltet. Controller-Test für diesen Ablauf ergänzt.
- Was verifiziert (mit Ausgabe): `node --test tests/*.test.cjs`: 37 Tests, 37 bestanden, 0 fehlgeschlagen. `npm run build:web`: „Fahrklar Web-PWA wurde nach dist gebaut.“ `gradle assembleDebug lintDebug`: lokaler Lauf bis zur Android-Kompilierung gestartet, aber die Build-Umgebung verlor die Verbindung; abschließende Verifizierung erfolgt über GitHub Actions.
- Was offen: Ergebnis der Android-/Web-CI-Läufe; schriftliche Fragenfreigabe; dauerhafter Release-Keystore.
- Entscheidung: Die Einrichtung zeigt Klassenwahl vor dem Download und blendet unbrauchbare Navigation bis zum vorhandenen Katalog aus.
- Warum: Neue Nutzer sehen genau den nächsten notwendigen Schritt und landen nicht auf leeren Lern- oder Prüfungsseiten.

## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Dauerhafte Android-Release-Signierung über die vorbereitete Einmal-Einrichtung aktiviert; privater Sicherungsordner außerhalb des Repositorys angelegt und die beiden vorgesehenen GitHub-Actions-Secrets gesetzt. Kein App-Code geändert.
- Was verifiziert (mit Ausgabe): GitHub Actions `https://github.com/paulschenkenfelder31-debug/Lern-App/actions/runs/34891440427`: Abschluss `success`; Jobs `build` und `release` erfolgreich; Schritte „Test learning and scoring logic“, „Build and lint APK“, „Package APK with persistent key when configured“ und „Publish downloadable APK“ erfolgreich. Release `version-27` veröffentlicht: `https://github.com/paulschenkenfelder31-debug/Lern-App/releases/tag/version-27`; Asset `Fahrklar.apk` vorhanden.
- Was offen: Privaten Sicherungsordner zusätzlich auf einem sicheren externen Datenträger oder in einem geeigneten verschlüsselten Backup sichern. Schriftliche Fragenfreigabe und veröffentlichte Web-Domain bleiben offen.
- Entscheidung: Den einmal erzeugten PKCS12-Keystore dauerhaft für alle Android-Releases verwenden und niemals in das Repository aufnehmen.
- Warum: Nur derselbe private Signierschlüssel ermöglicht Android-Updates unter Erhalt der App-Daten; GitHub Actions erhält ihn ausschließlich über verschlüsselte Secrets.

## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Wiederholungsplan um den nächsten Fälligkeitszeitpunkt, eine hervorgehobene Terminanzeige und einen eindeutigen Sofortstart für fällige Fehler erweitert. Lernpfad zeigt den nächsten Termin ebenfalls an. Controller-Test ergänzt.
- Was verifiziert (mit Ausgabe): `node --test tests/*.test.cjs`: 38 Tests, 38 bestanden, 0 fehlgeschlagen. `npm run build:web`: „Fahrklar Web-PWA wurde nach dist gebaut.“ `node --check app/src/main/assets/app.js`: erfolgreich ohne Ausgabe. Android-Verifizierung erfolgt zusätzlich im Release-Workflow.
- Was offen: Schriftliche Antwort von F-Online; Ergebnis des Android-/Web-Release-Workflows für diese Änderung; externer Keystore-Backup.
- Entscheidung: Fälligkeiten aus den bestehenden versionierten Antworten berechnen, ohne neues Speicherformat oder neue Laufzeitabhängigkeit.
- Warum: Der Nutzer erkennt sofort, ob und wann eine Wiederholung ansteht; bestehende Sicherungen und Cloud-Daten bleiben kompatibel.

## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Android-Workflow setzt die benötigten SDK-Pakete ausdrücklich und verlangt nicht mehr das entfernte Legacy-Paket `tools`; Release-Hinweis auf ersten Start und Wiederholungsplan aktualisiert.
- Was verifiziert (mit Ausgabe): Fehlgeschlagener Lauf `34893468160`: 38 Logiktests erfolgreich; Fehler ausschließlich in `android-actions/setup-android@v3` mit „Failed to find package 'tools'“, bevor Gradle gestartet wurde. Web-Lauf `34893468063`: `success`.
- Was offen: Erneuter Android-Release-Lauf mit expliziten SDK-Paketen.
- Entscheidung: Bestehende Setup-Action beibehalten und ihre Pakete explizit auf `platform-tools`, `platforms;android-35` und `build-tools;35.0.0` begrenzen.
- Warum: Der GitHub-Runner bietet das veraltete Paket `tools` nicht mehr an; die App benötigt ausschließlich die konkret genannten SDK-Komponenten.
