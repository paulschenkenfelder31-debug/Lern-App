## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Gemeinsame Arbeitsregeln in `AGENTS.md`, Claude-Verweis in `CLAUDE.md` und konfliktarme getrennte Protokollführung unter `docs/protokoll/` angelegt.
- Was verifiziert (mit Ausgabe): Repository-Dateien und bestehende CI-Konfiguration geprüft. In `.github/workflows/android.yml` stehen die Android-Tests und der APK-Build; in `.github/workflows/web.yml` stehen `npm test` und `npm run build:web`. Die letzten bekannten Android- und Web-Läufe waren erfolgreich.
- Was offen: Schriftliche Nutzungsgenehmigung für den Fragenkatalog; dauerhafter Release-Keystore; veröffentlichte Web-Domain für zusätzliche Firebase-Domainfreigabe.
- Entscheidung: Pro Werkzeug eine eigene Tagesdatei statt einer gemeinsamen Protokolldatei.
- Warum: Parallele Anhänge erzeugen sonst unnötige Git-Konflikte; getrennte append-only Dateien bleiben konfliktarm und trotzdem nachvollziehbar.

## Datum: 2026-09-14
- Wer: Codex
- Was geändert: Themenauswahl als fokussiertes Akkordeon verbessert; beim Öffnen einer Gruppe schließen sich andere Gruppen automatisch.
- Was verifiziert (mit Ausgabe): Patch auf `app/src/main/assets/app.js` erfolgreich veröffentlicht; GitHub meldete Commit `36fae5c75ef87983f80399b0e2cc107c0fed6bcc`.
- Was offen: Android-/Web-CI-Läufe dieses neuen Commits; Release-Keystore und Fragenfreigabe bleiben offen.
- Entscheidung: Kleine, barrierearme UX-Verbesserung ohne neue Abhängigkeit.
- Warum: Lange Themenlisten werden schneller scanbar und die Auswahl bleibt auf Mobilgeräten übersichtlich.
