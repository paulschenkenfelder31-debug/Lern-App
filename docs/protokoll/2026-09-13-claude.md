## Datum: 2026-09-13
- Wer: Claude Code, Sitzung mit David
- Was geändert: `scripts/ui-baseline.mjs` geschrieben. Es fotografiert 70 Zustände bei 390 × 844 mit synthetischem Katalog im Speicher, fester Uhrzeit, festem Zufall und `performance.now` auf 0, und vergleicht SHA-256-Prüfsummen gegen `tests/ui-baseline/`. Playwright 1.58.2 mit `--no-save`, `package.json` bleibt ohne Pakete. Git-Historie auf eingecheckte Kataloginhalte geprüft. IFG-Antrag an `servicebuero@bmimi.gv.at` und Genehmigungsanfrage an `info@f-online.at` aus `daveghg03@gmail.com` verschickt.
- Was verifiziert (mit Ausgabe): Zwei Vergleichsläufe hintereinander je „Unverändert: 70", Exit-Code 0. Gegentest mit angehängtem `.card{outline:3px solid #ff0000}`: 53 Bilder geändert, Exit-Code 1, `style.css` danach per `git checkout` zurückgesetzt. Git-Historie über 20 Commits: hinzugefügte JSON- und Bilddateien nur `firebase.json`, `package.json`, zwei App-Icons; keine Datei über 200 KB; `txt_text` nur als Feldname im Code und in synthetischen Tests. Gmail-Nachrichten-IDs `1a09a56048c6dd77` und `1a09a561fc53c90d`.
- Was offen: Ob die 13 MB Referenzbilder ins öffentliche Repository gehören. Antworten auf beide E-Mails.
- Entscheidung: Referenzbilder vorerst nicht committen, nur das Skript.
- Warum: Git behält jede Bildversion dauerhaft; das ist eine Absprache zwischen Paul und David, keine Entscheidung eines Werkzeugs.

## Datum: 2026-09-13
- Wer: Claude Code, Sitzung mit David
- Was geändert: Lernrunde als Vollbild und einschichtiges Designsystem nach Spec `docs/superpowers/specs/2026-09-13-lernrunde-designsystem-design.md` und Plan `docs/superpowers/plans/2026-09-13-lernrunde-designsystem.md`. In `style.css` nur noch eine `:root`- und eine `body.dark`-Definition mit den Tokens `--bg --surface --text --text-muted --line --accent --accent-strong --accent-soft --on-accent --ok --ok-soft --bad --bad-soft --reward --radius`; die vier Farbthemen setzen nur `--accent`, `--accent-strong`, `--accent-soft`. Lernrunde ohne Markenkopf, schlanke Leiste mit ✕, Zähler, Fortschritt und Zeitknopf als Pause, Antworten mit Buchstaben, Kennzeichnungen „Richtig", „Richtig · übersehen", „Falsch gewählt", Satz wie „A war richtig, B war falsch.", angedocktes Feld für Urteil und Hauptknopf, Gemini eingeklappt. `render()` setzt `data-register` an `#app` und `in-session` am `body`. Neu `tests/contrast.test.cjs` und vier Controller-Tests.
- Was verifiziert (mit Ausgabe): Kontrasttest vor der Umstellung `ℹ fail 9`. Nach Umsetzung `node --test tests/*.test.cjs`: `ℹ tests 50`, `ℹ pass 50`, `ℹ fail 0`. Hauptknopf im Dunkelmodus vorher weiß auf `#55d596` mit 1,85 : 1, jetzt `#10151f` auf `#55d596` mit 9,87 : 1. Playwright-Messung mit langer Frage und vier Antworten, hell und dunkel, in Frage, Auswahl und falscher Auflösung: sechsmal `OK`, Hauptknopf bei 780 bis 832 px innerhalb von 844 px, Frage beginnt bei 128 px. Neue Referenz: „70 Referenzbilder in tests/ui-baseline geschrieben.", danach „Unverändert: 70". Android nicht ausgeführt, lokal kein SDK.
- Was offen: Android-Build in der CI. `color-mix()` im Knopfschatten auf einer älteren Android System WebView prüfen. Prüfungsnavigation mit Fragenraster, Aufteilen von `app.js` und Umbau von Start, Pfad und Fortschritt sind nicht Teil dieser Arbeit.
- Entscheidung: Regeln des ruhigen Registers unter `#app[data-register="calm"]`; Lernrunde als Flex-Spalte mit `min-height: 100dvh`, damit das angedockte Feld am unteren Rand sitzt. Beides weicht vom Plan ab.
- Warum: Ohne ID gewannen spätere Regeln `.answer.correct` und `.answer.wrong` des verspielten Stils bei gleicher Spezifität und brachten Blockschatten zurück. Mit `position: sticky` allein stand das Feld bei kurzen Fragen mitten im Bildschirm.
