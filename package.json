{
  "name": "fahrklar",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build-web.mjs",
    "build:web": "node scripts/build-web.mjs",
    "test": "node --test tests/*.test.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
