# Fahrklar

Ziele, Regeln, Stil, Ablage und das Format des Arbeitsprotokolls stehen in [`AGENTS.md`](AGENTS.md). Diese Datei enthält bewusst keine eigenen Regeln, damit Claude Code und Codex dieselbe Grundlage lesen.

Lies `AGENTS.md` vollständig, bevor du etwas änderst. Lies danach die letzten zwei bis drei Einträge in `docs/log/`.
